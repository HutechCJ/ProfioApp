import { generatedProfioApiTypes } from './generated-profio.api-types';

describe('generatedProfioApiTypes', () => {
  it('should work', () => {
    expect(generatedProfioApiTypes()).toEqual('generated-profio.api-types');
  });
});
