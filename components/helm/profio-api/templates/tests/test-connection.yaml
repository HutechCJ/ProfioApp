apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profio-api.fullname" . }}-test-connection"
  labels:
    {{- include "profio-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profio-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
