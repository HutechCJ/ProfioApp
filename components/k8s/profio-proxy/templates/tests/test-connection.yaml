apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profio-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "profio-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profio-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
